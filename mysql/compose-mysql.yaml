version: '3.1'
services:
  mysql:
    image: mysql:latest
    container_name: mysql
    privileged: true #一定要设置为true，不然数据卷可能挂载不了，启动不起
    ports: 
     - 45000:3306
    environment:
      TZ: Asia/Shanghai
      MYSQL_USER: root
      MYSQL_PASSWORD: 1vo#X^E$g4)uYMQD
      MYSQL_RANDOM_ROOT_PASSWORD: 1vo#X^E$g4)uYMQD
      MYSQL_ROOT_PASSWORD: 1vo#X^E$g4)uYMQD # 自己配置数据库密码
    command:
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_general_ci
      --explicit_defaults_for_timestamp=true
      --lower_case_table_names=1
      --max_allowed_packet=128M  
    volumes:
      - ./data:/var/lib/mysql
      - ./conf/my.cnf:/etc/my.cnf
    restart: always